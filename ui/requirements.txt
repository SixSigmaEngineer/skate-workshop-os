fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
python-frontmatter>=1.1
markdown>=3.5
pygments>=2.17
pystray>=0.19
pillow>=10.0
pywebview>=5.0
imageio-ffmpeg>=0.4.9
mcp>=1.23,<2

# Optional: semantic retrieval (ui/embeddings.py). Preferred: install Ollama
# (https://ollama.com, MIT) and run `ollama pull nomic-embed-text` — zero pip
# deps, GPU-accelerated. Alternative, CPU-only, no server:
#   pip install fastembed
# fastembed>=0.3
# Without either, SKATE silently falls back to lexical (keyword) search.

# Optional: PDF support for the OneNote importer. Word (.docx), Markdown, HTML,
# and TXT imports work with the standard library and need nothing here. Install
# this only if you want to import OneNote pages that were exported to PDF:
#   pip install pypdf
# pypdf>=4.0

# Optional: local/offline Whisper transcription. NOT needed when using ElevenLabs
# Scribe for Speech-to-Text. It pulls in numba/torch, which do not support
# Python 3.15+ (only 3.10-3.13). To enable offline Whisper, use Python 3.13 and run:
#   pip install openai-whisper
# openai-whisper>=20250625
